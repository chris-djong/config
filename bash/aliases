# Aliases
alias vi="nvim"
alias vim="nvim"
alias cd="z"
# Only needed for debian distros
if command -v batcat >/dev/null 2>&1; then
  alias bat="batcat"
fi
alias ll="ls -al"
alias set_env=". .venv/bin/activate"
alias pbox="boxes -s 74x1 -a c"
